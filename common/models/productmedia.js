'use strict';

module.exports = function (Productmedia) {

    // Productmedia.beforeRemote("prototype.patchAttributes", async (ctx, modelInstance, next) => {
    //     console.log(ctx);

    //     try {
    //         if (ctx.args.data.id) {

    //         }
    //     } catch (error) {

    //     }
    // });

};
